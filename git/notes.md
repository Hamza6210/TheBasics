# Day 2 - Git and GitHub Basics

## What I learned
- The 3 stages: untracked, staged, committed
- git status, git add, git commit -m, git push, git pull
- Branches: git branch, git checkout to switch
- Merging branches with git merge
- Merge conflicts: how to read and resolve <<<<<<< ======= >>>>>>>
- git log to view commit history
- Writing a clean README.md for a repo
